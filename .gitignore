# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Swift sidecar + compiled binaries — regenerated by scripts/build-sidecar.sh
# (predev/prebuild), not source
src-tauri/sidecar/*/.build
src-tauri/binaries

# Release .dmg copied to the project root for convenience — the real build
# output lives under src-tauri/target and is regenerated by `tauri build`
/Taski.dmg

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
